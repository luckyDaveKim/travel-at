/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/travel-at",
    output: "export",
    reactStrictMode: true,
    images: { unoptimized: true }
};

export default nextConfig;
